import React from 'react';
import './style.scss';
const Card = () => {
  return (
    <div className="Card" onClick={() => {}}>
      {/* <img className="Card__img" src={data.urls.thumb} alt={data.urls.name} /> */}
    </div>
  );
};

export default Card;
